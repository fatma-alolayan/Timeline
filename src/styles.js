import styled from "styled-components";

const Title = styled.h1`
  text-align: center;
`;
const LineStyle = styled.div`
  color: #e86971;
  font-size: 24px;

  bodycontainerstyle: {
    background: #ddd;
    padding: 20px;
    borderradius: 8px;
    boxshadow: 0.5rem 0.5rem 2rem 0 rgba(0, 0, 0, 0.2);
  }
`;
export { Title, LineStyle };
